version: 1
frontend:
  phases:
    build:
      commands:
        - echo "No build required - static HTML site"
  artifacts:
    baseDirectory: /
    files:
      - dental-landing-page.html
      - '**/*'
  cache:
    paths: []
